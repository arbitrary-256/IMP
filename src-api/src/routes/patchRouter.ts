// /** @format */
// import { Response, Request, Router } from 'express'

// export const patchRouter: Router = Router()
// patchRouter.route(`*`)
// patchRouter
//   .get(`/`, (request: Request, response: Response) => response.status(200).send(`PATCH impAPI/`))
//   .patch(`/patch`, (request: Request, response: Response) => response.status(200).send(`PATCH impAPI/patch`))
// export { }
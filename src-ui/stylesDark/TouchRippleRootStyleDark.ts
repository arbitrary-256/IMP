import { CSSProperties } from '@mui/styles'
import { grey } from '@mui/material/colors'
export const TouchRippleRootStyleDark: Function = (): CSSProperties => {
  return {
  color: grey[600],
  borderRadius: `24px`
}
}

import { CSSProperties } from '@mui/styles'
import { grey } from '@mui/material/colors'
export const MobileStepperDotActiveStyle: Function = (): CSSProperties => {
  return {
    backgroundColor: grey[800]
  }
}
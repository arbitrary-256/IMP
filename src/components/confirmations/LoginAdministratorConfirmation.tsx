/** @format */
/**
 * @returns a React.FC that confirms logging in as an administrator
 */
export const LoginAdministratorConfirmation: React.FC = (): React.ReactElement => {
  return (
    <div className={`Confirmation`}>
      <p>Successfully logged into IMP as Administrator.</p>
    </div>
  )
}

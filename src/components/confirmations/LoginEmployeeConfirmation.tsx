/** @format */
/**
 * @returns a React.FC that confirms logging in as an employee
 */
export const LoginEmployeeConfirmation: React.FC = (): React.ReactElement => {
  return (
    <div className={`Confirmation`}>
      <p>Successfully logged into IMP as Employee.</p>
    </div>
  )
}

/** @format */
/**
 * @returns a React.FC that indicates Administrator is logged in
 */
export const LoginAdministratorView: React.FC = (): React.ReactElement => {
  return (
    <div className={`LoginAdmin`}>
      <p>Administrators must logout when they're done using IMP.</p>
    </div>
  )
}

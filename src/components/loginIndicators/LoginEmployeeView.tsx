/** @format */
/**
 * @returns a React.FC that indicates Employee is logged in
 */
export const LoginEmployeeView: React.FC = (): React.ReactElement => {
  return (
    <div className={`LoginEmployee`}>
      <p>Employees should logout when they're done using IMP.</p>
    </div>
  )
}

import { CSSProperties } from '@mui/styles'
import { blue } from '@mui/material/colors'
export const ButtonSecondaryStyleDark: Function = (): CSSProperties => {
  return {
  backgroundColor: blue[500]
}
}

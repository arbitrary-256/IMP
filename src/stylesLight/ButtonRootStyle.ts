import { CSSProperties } from '@mui/styles'
export const ButtonRootStyle: Function = (): CSSProperties => {
  return {
  borderRadius: `24px`,
  margin: `7px`,
  textTransform: `lowercase`,
  boxShadow: `none`
}
}

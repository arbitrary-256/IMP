# the host address of the mariadb or mysql server
HOST=your.mariadb.host
# the port your mariadb or mysql server is listening on - defaults to 3306
PORT=3306
# the root username to connect to the mariadb or mysql server
IMPROOTUSERNAME=improotuser
# the root password to connect to the mariadb or mysql server
IMPROOTPASSWORD=improotpassword
# the user name to authenticate as
USER=your_username
# the password to use to authenticate
PASSWORD=your_password
# the database to connect to
DATABASE=your_database_name
